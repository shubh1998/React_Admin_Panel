import { styled } from '@mui/material'

export const LoginContainer = styled('div')(({ theme }) => ({
  display: 'grid',
  padding: '4%',
  margin: '10%',
  justifyContent: 'center',
  textAlign: 'center'
}))
